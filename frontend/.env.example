# GraphQL API Endpoint
# For local development, use the default (relative path)
# For production (Netlify), set this to your backend URL
VITE_GRAPHQL_ENDPOINT=/graphql

# Example for production:
# VITE_GRAPHQL_ENDPOINT=https://your-backend-api.com/graphql
